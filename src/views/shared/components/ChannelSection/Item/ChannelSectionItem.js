import React from 'react';
import styled from 'styled-components';

const ChannelSectionItem = ({ item }) => (
  <Container>
    ChannelSectionItem
  </Container>
)

const Container = styled.div`

`;
export default ChannelSectionItem;
